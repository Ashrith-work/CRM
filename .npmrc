# Expo / React Native and some Nest plugins resolve dependencies more reliably
# with a hoisted node_modules layout than with pnpm's default symlinked one.
node-linker=hoisted
shamefully-hoist=true
strict-peer-dependencies=false
auto-install-peers=true
